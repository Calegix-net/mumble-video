# QA hosts for mumble-video. The default target is the machine you run this on
# (connection: local), which is what the agents box uses. Add remote hosts under
# [qa] with ansible_host/ansible_user to drive a colocated client on the server LAN.
[qa]
localhost ansible_connection=local
